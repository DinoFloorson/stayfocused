import styled from '@emotion/styled';
import PrimaryButton from './PrimaryButton';

const MethodButton = styled(PrimaryButton)`
  width: 90px;
  height: 27px;
  border-radius: 12px;
`;

export default MethodButton;
